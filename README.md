# Next_Token_yz
next token prediction with small language datasets

Tokenizers are in \Tokenizer. 

run Dataset.py main to train the tokenizer. 

run traincpu.py to train the model. -> init_from = "ufm" for UFM and init_from = "tfm" for transformer. 
