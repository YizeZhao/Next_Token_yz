# Next_Token_yz
next token prediction with small language dataset

Tokenizer trained on verysmalldataset.txt is in \Tokenizer. 

run Dataset.py main to train the tokenizer. 

run train.py to train the model. -> init_from = "mlp" for fixed length mlp and init_from = "tfm" for transformer. 
